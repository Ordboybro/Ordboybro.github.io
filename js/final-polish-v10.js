(() => {
  'use strict';
  // Legacy V10 runtime intentionally disabled.
  // This prevents old MutationObservers, overlay remounting and inline CSS from
  // fighting the consolidated runtime and router.
})();
