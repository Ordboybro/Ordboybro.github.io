(()=>{'use strict';
/* Emoji Drops action resilience v6: normalize empty action markers, deterministic activation, modal lifecycle and keyboard focus safety. No polling. */
const KEY='emojiDropsStateV3',MARK='emojiDropsActionResilienceV6',RETRY_DELAY=720;
const ACTIONS=['data-do-open','data-sell','data-sell-all','data-upgrade','data-buy','data-list-random','data-daily','data-reset'];
const cloneState=()=>{try{const s=JSON.parse(localStorage.getItem(KEY)||'null');return s&&typeof s==='object'?{opens:Number(s.stats?.opens)||0,inventory:Array.isArray(s.inventory)?s.inventory.length:0,balance:Number(s.balance)||0}:null}catch{return null}};
const changed=(a,b)=>!!a&&!!b&&(a.opens!==b.opens||a.inventory!==b.inventory||a.balance!==b.balance);
function normalize(root=document){const nodes=root.querySelectorAll?.('[data-do-open],[data-sell],[data-sell-all],[data-upgrade],[data-buy],[data-list-random],[data-daily],[data-reset]')||[];for(const node of nodes)for(const attr of ACTIONS)if(node.hasAttribute(attr)&&node.getAttribute(attr)==='')node.setAttribute(attr,'1')}
let last=0;
function verifyAndRetry(button,before){if(!button?.isConnected)return;const after=cloneState();if(changed(before,after))return;if(button.dataset.edRetry==='1')return;button.dataset.edRetry='1';last=Date.now();button.click();setTimeout(()=>{if(button.isConnected)delete button.dataset.edRetry},900)}
function activateFromPointer(e){const button=e.target?.closest?.('[data-do-open]');if(!button||Date.now()-last<120)return;const before=cloneState();if(!before)return;if(button.dataset.edPointer==='1')return;button.dataset.edPointer='1';last=Date.now();button.click();setTimeout(()=>{if(button.isConnected)delete button.dataset.edPointer},900);setTimeout(()=>verifyAndRetry(button,before),RETRY_DELAY)}
function onClick(e){const button=e.target?.closest?.('[data-do-open]');if(!button||Date.now()-last<120)return;const before=cloneState();if(!before)return;setTimeout(()=>verifyAndRetry(button,before),RETRY_DELAY)}
function visibleModal(){return [...document.querySelectorAll('.ed-modal.show')].find(m=>{const r=m.getBoundingClientRect(),s=getComputedStyle(m);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'})||null}
function closeModal(modal){if(!modal)return false;modal.classList.remove('show');modal.setAttribute('aria-hidden','true');modal.style.removeProperty('display');document.body.classList.remove('ed-modal-open');const trigger=window.__emojiDropsLastModalTrigger;if(trigger?.isConnected){try{trigger.focus()}catch{}}return true}
function modalClick(e){const close=e.target?.closest?.('.ed-close,[data-close]');if(close){const modal=close.closest('.ed-modal');if(modal){e.preventDefault();e.stopPropagation();closeModal(modal)}}}
function modalKeydown(e){if(e.key==='Escape'){const modal=visibleModal();if(modal){e.preventDefault();e.stopPropagation();closeModal(modal);return}}const modal=visibleModal();if(!modal||e.key!=='Tab')return;const focusables=[...modal.querySelectorAll('button,input,select,textarea,a[href],[tabindex]:not([tabindex="-1"])')].filter(x=>!x.disabled&&x.offsetParent!==null);if(!focusables.length)return;const first=focusables[0],lastEl=focusables[focusables.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();lastEl.focus()}else if(!e.shiftKey&&document.activeElement===lastEl){e.preventDefault();first.focus()}}
function modalOpenObserver(){const observer=new MutationObserver(()=>{for(const modal of document.querySelectorAll('.ed-modal.show')){if(!modal.hasAttribute('aria-hidden'))modal.setAttribute('aria-hidden','false');if(!window.__emojiDropsLastModalTrigger&&document.activeElement instanceof HTMLElement)window.__emojiDropsLastModalTrigger=document.activeElement}});observer.observe(document.documentElement,{attributes:true,subtree:true,attributeFilter:['class']})}
const install=()=>{normalize();const observer=new MutationObserver(()=>normalize());observer.observe(document.documentElement,{childList:true,subtree:true});modalOpenObserver();document.addEventListener('pointerup',activateFromPointer,true);document.addEventListener('click',onClick,true);document.addEventListener('click',modalClick,true);document.addEventListener('keydown',modalKeydown,true)};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
window.__emojiDropsActionResilience={version:6,key:MARK,retryDelay:RETRY_DELAY,verifyAndRetry,normalize,closeModal};
})();
