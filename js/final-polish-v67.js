(()=>{'use strict';
const V='20260902-67';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const PRICE={transport:25,animals:40,food:60,nature:85,moves:110,smile:140,sport:180,games:230,space:290,ocean:360,flags:420};
const MULT={common:.30,rare:.55,epic:1,mythical:1.7,legendary:3};
const RANK=['common','common','common','common','common','common','common','common','common','common','common','common','common','common','rare','rare','rare','rare','rare','rare','rare','rare','rare','epic','epic','epic','epic','epic','mythical','mythical','mythical','mythical','legendary'];
const num=x=>Number(x?.value??x?.price??0)||0;
const users=()=>{try{return JSON.parse(localStorage.getItem('users')||'[]')}catch{return[]}};
const current=()=>{let raw;try{raw=JSON.parse(localStorage.getItem('currentUser')||'null')}catch{raw=localStorage.getItem('currentUser')||''}if(!raw)return null;const id=typeof raw==='string'?raw:(raw.email||raw.id||raw.username||raw.nickname||'');return users().find(u=>u.email===id||u.id===id||u.username===id||u.nickname===id)||null};
const save=u=>{const a=users(),i=a.findIndex(x=>(u.email&&x.email===u.email)||(u.id&&x.id===u.id));if(i>=0)a[i]=u;else a.push(u);localStorage.setItem('users',JSON.stringify(a))};
const allItems=()=>Object.values(window.cases||window.CASES||{}).flat().filter(Boolean);
function canonicalEconomy(){const c=window.cases;if(!c)return;for(const k of Object.keys(PRICE)){if(!Array.isArray(c[k]))continue;c[k].forEach((it,i)=>{it.caseId=k;const rarity=it.rarity||RANK[i]||'common';it.rarity=rarity;const spread=.92+(Math.min(i,34)/34)*.16;it.value=Math.max(1,Math.round(PRICE[k]*(MULT[rarity]||.30)*spread));it.price=`${it.value}₽`})}window.casePrices={...(window.casePrices||{}),...PRICE};document.documentElement.dataset.economy='canonical-v7'}
function home(){const p=location.pathname.toLowerCase();return p==='/'||p.endsWith('/index.html')||p.endsWith('/ordboybro.github.io/')}
function cleanLayout(){canonicalEconomy();document.documentElement.classList.remove('ed-ios');$('.hero')?.remove();$('.ed64-banner')?.remove();$$('.ed64-line').forEach(x=>x.remove());const grid=$('#cases,.cases');if(grid){grid.style.display='grid';grid.style.gridTemplateColumns='repeat(4,minmax(0,1fr))';grid.style.gap='18px'}$$('.case-tile .case-card').forEach(c=>{c.style.width='100%';c.style.minWidth='0';c.style.pointerEvents='auto'});const live=$('.live-section');if(live){live.hidden=!home();live.style.position='relative';live.style.inset='auto';live.style.width='100%';live.style.margin='28px 0 0'}$$('.live-drop').forEach(x=>{x.style.position='relative';x.style.inset='auto'});const profile=$('#profileModal');if(profile?.classList.contains('show')&&live)live.hidden=true;$$('.upgrade-v2-wheel,.upgrade-wheel-shell,.ed-upgrade-stage').forEach(x=>x.remove());const best=$('.best-wrap');if(best)$$('button',best).filter(b=>/продать\s*все/i.test(b.textContent)).forEach(b=>b.remove());const inv=$('.inventory');if(inv&&!$('.ed67-sellall',inv)){const h=$('.section-title',inv);if(h){const b=document.createElement('button');b.className='ed67-sellall';b.textContent='Продать всё';b.type='button';b.onclick=sellAll;h.after(b)}}syncFunds()}
function syncFunds(){const u=current(),modal=$('.case-modal.show,.case-modal[aria-hidden="false"],#caseModal.show,#openModal.show');if(!u||!modal)return;const key=window.currentCaseKey,price=Number(window.casePrices?.[key])||0;$$('button',modal).forEach(b=>{if(/открыть/i.test(b.textContent)&&price)b.disabled=(Number(u.balance)||0)<price})}
function sellAll(){const u=current();if(!u)return;u.inventory=Array.isArray(u.inventory)?u.inventory:[];const total=Math.round(u.inventory.reduce((s,i)=>s+num(i),0));u.balance=Math.max(0,Math.round(Number(u.balance)||0))+total;u.inventory=[];u.stats=u.stats||{};u.stats.received=(Number(u.stats.received)||0)+total;save(u);typeof window.renderAll==='function'?window.renderAll():location.reload()}
let source=null,target=null,mult=2,busy=false,angle=0;
const chanceFor=(s,t,m)=>{s=num(s);t=num(t);m=Math.max(1,Number(m)||1);if(!(s>0&&t>s&&t<=s*m))return 0;return Math.max(0,Math.min(100,((s*m-t)/(s*m-s))*100))};
const key=i=>`${i.caseId||''}|${i.emoji||i.icon||''}|${num(i)}|${i.rarity||''}`;
function upgradeModal(){let m=$('#ed67-upgrade');if(m)return m;m=document.createElement('div');m.id='ed67-upgrade';m.className='ed67-modal';m.innerHTML='<div class="ed67-upgrade-panel"><button class="ed67-close" type="button">×</button><h2>Upgrade</h2><div class="ed67-sub">Выбери предмет → цель дороже → множитель</div><div class="ed67-grid"><section><h3>Твой предмет</h3><div id="ed67-source" class="ed67-items"></div></section><section class="ed67-wheel-col"><div class="ed67-pointer"></div><div id="ed67-wheel" class="ed67-wheel"><div class="ed67-wheel-center">UP</div><b>WIN</b><span>LOSE</span></div><div id="ed67-chance" class="ed67-chance">Выбери предмет</div><div class="ed67-mults"><button data-m="1.5">×1.5</button><button data-m="2">×2</button><button data-m="3">×3</button><button data-m="5">×5</button></div><button id="ed67-go" class="ed67-go" type="button" disabled>Upgrade</button></section><section><h3>Цель</h3><div id="ed67-target" class="ed67-items"></div></section></div></div>';document.body.appendChild(m);$('.ed67-close',m).onclick=closeUpgrade;$$('[data-m]',m).forEach(b=>b.onclick=()=>{mult=Number(b.dataset.m);$$('[data-m]',m).forEach(x=>x.classList.toggle('active',x===b));target=null;renderTargets()});$('#ed67-go',m).onclick=doUpgrade;return m}
function itemCard(i,side){const b=document.createElement('button');b.type='button';b.className='ed67-item';b.innerHTML=`<span>${i.emoji||i.icon||'🎁'}</span><strong>${Math.round(num(i))}₽</strong><small>${String(i.rarity||'common').toUpperCase()}</small>`;b.onclick=()=>{if(busy)return;if(side==='source'){source=i;target=null;renderSources();renderTargets()}else{target=i;renderTargets();updateChance()}};return b}
function renderSources(){const box=$('#ed67-source');if(!box)return;box.innerHTML='';const inv=current()?.inventory||[];inv.slice().sort((a,b)=>num(a)-num(b)).forEach(i=>{const c=itemCard(i,'source');if(source&&key(source)===key(i))c.classList.add('selected');box.appendChild(c)});if(!inv.length)box.innerHTML='<p class="ed67-empty">Инвентарь пуст</p>'}
function renderTargets(){const box=$('#ed67-target');if(!box)return;box.innerHTML='';if(!source){box.innerHTML='<p class="ed67-empty">Сначала выбери предмет</p>';updateChance();return}const max=num(source)*mult,seen=new Set();allItems().filter(i=>num(i)>num(source)&&num(i)<=max).sort((a,b)=>num(a)-num(b)).forEach(i=>{const k=key(i);if(seen.has(k))return;seen.add(k);const c=itemCard(i,'target');if(target&&key(target)===k)c.classList.add('selected');box.appendChild(c)});if(!box.children.length)box.innerHTML='<p class="ed67-empty">Нет целей в диапазоне</p>';updateChance()}
function updateChance(){const el=$('#ed67-chance'),go=$('#ed67-go');if(!el)return;if(!source){el.textContent='Выбери предмет';go.disabled=true;return}const max=num(source)*mult;if(!target||num(target)<=num(source)||num(target)>max){el.textContent=`Цель: дороже ${Math.round(num(source))}₽ и не дороже ${Math.round(max)}₽`;go.disabled=true;return}el.textContent=`Шанс: ${chanceFor(source,target,mult).toFixed(1)}%`;go.disabled=busy}
function doUpgrade(){if(busy||!source||!target)return;const u=current();if(!u)return;u.inventory=Array.isArray(u.inventory)?u.inventory:[];const si=u.inventory.findIndex(i=>key(i)===key(source));if(si<0)return;const sv=num(source),tv=num(target);if(tv<=sv||tv>sv*mult)return;busy=true;$('#ed67-go').disabled=true;const chance=chanceFor(sv,tv,mult)/100;angle+=1440+Math.random()*360;$('#ed67-wheel').style.transform=`rotate(${angle}deg)`;setTimeout(()=>{const win=Math.random()<chance;u.inventory.splice(si,1);u.stats=u.stats||{};u.stats.upgrades=(Number(u.stats.upgrades)||0)+1;if(win)u.inventory.push(JSON.parse(JSON.stringify(target)));save(u);busy=false;source=null;target=null;renderSources();renderTargets();window.renderAll?.();$('#ed67-chance').textContent=win?`WIN — ${Math.round(tv)}₽`:'LOSE'},5200)}
function openUpgrade(){const m=upgradeModal();m.classList.add('show');source=null;target=null;mult=2;busy=false;$$('[data-m]',m).forEach(x=>x.classList.toggle('active',x.dataset.m==='2'));renderSources();renderTargets()}
function closeUpgrade(){$('#ed67-upgrade')?.classList.remove('show')}
window.openUpgrade=openUpgrade;window.closeUpgrade=closeUpgrade;
function guardOpen(){document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b||!/^\s*открыть/i.test(b.textContent||''))return;const modal=b.closest('.case-modal,#caseModal,#openModal,.modal');if(!modal)return;const price=Number(window.casePrices?.[window.currentCaseKey])||0,u=current();if(price&&u&&Number(u.balance||0)<price){e.preventDefault();e.stopImmediatePropagation();b.disabled=true;let x=modal.querySelector('.ed67-insufficient');if(!x){x=document.createElement('div');x.className='ed67-insufficient';x.textContent=`Недостаточно средств — нужно ${price}₽`;modal.appendChild(x)}x.hidden=false}},true)}
function loadCss(){if(document.querySelector('link[data-ed-v67-css]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href=`css/final-polish-v67.css?v=${V}`;l.dataset.edV67Css='1';document.head.appendChild(l)}
function observe(){let timer=0;const run=()=>{clearTimeout(timer);timer=setTimeout(cleanLayout,40)};new MutationObserver(m=>{if(m.some(x=>x.type==='childList'))run()}).observe(document.body,{childList:true,subtree:true});window.addEventListener('resize',run);window.addEventListener('pageshow',run);window.addEventListener('emoji-drops-ready',run)}
function boot(){loadCss();cleanLayout();if(typeof window.renderAll==='function'){const old=window.renderAll;window.renderAll=function(){const r=old.apply(this,arguments);setTimeout(cleanLayout,0);return r}}guardOpen();observe()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
