/*
 * Emoji Drops — Supabase client configuration.
 *
 * This file is safe to publish with the browser anon/publishable key.
 * NEVER put a Supabase service_role/secret key here.
 *
 * Create a free Supabase project and replace the two empty strings below.
 */
window.EMOJI_DROPS_SUPABASE = Object.freeze({
  url: '',
  anonKey: ''
});
