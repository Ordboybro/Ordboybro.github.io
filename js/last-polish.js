(() => {
  'use strict';
  // Legacy last-polish runtime intentionally disabled.
  // It previously created synthetic drops, rebuilt roulette DOM and installed
  // a permanent MutationObserver/timer over the whole application.
})();
